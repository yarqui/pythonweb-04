## Usage

Run the script from your terminal, providing the source and destination directories as arguments.

**Important:** Execute the command from the root directory of your project.

```bash
poetry run python main.py -src <SOURCE_FOLDER> -dest <DESTINATION_FOLDER>